#include "main.h"
#include <math.h>
/**
*_abs - Print and checks the absolute of a number
*@i: The character to be checked.
* Return:the number if its
* greater than 0 and multiply the number by itself and devi
*/
int _abs(int i)
{
	if (i < 0)
	{
	return (-i);
	}
	else
	{
	return (i);
	}
}
