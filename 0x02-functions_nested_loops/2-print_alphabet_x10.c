#include "main.h"
/**
 * print_alphabet_x10 -  this function prints the
* alphabet in lowercase 10 times , followed by a new line.
*/
int _islower(int c)
{
	if (c >= 'a' && c <= 'z' )
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
