#include "main.h"
#include <stdio.h>

/**
 * mul - taking in to numbers and multiplies them .
 * @b: the charactor to be checked.
 * @a: the number to be multiplied
 * Return: 1 if character is lowercase, 0 otherwise.
 */

int mul(int a, int b)
{
	return (a * b);
}
