#include<stdio.h>
/* more headers goes there */


/**
*main - Entry point
*
*Return: Always 0 (Success)
*/

int main(void)
	{
	char letas;

	for (letas = 'a' ; letas <= 'z' ; letas++)
	{
		putchar(letas);
	}
	return (0);
}
